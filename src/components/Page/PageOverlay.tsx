import "./Page.css";

interface Props {
  hash: string;
  title?: string;
}
export const PageOverlay: React.FC<Props> = ({ title, children, hash }) => {
  

  return (
    <div>
    </div>
  );
};

export default PageOverlay;
