import React from "react";
import "./InsideContainer.scss";

export const InsideContainer: React.FC = ({ children }) => {
  return <div className="InsideContainer">{children}</div>;
};

export default InsideContainer;
